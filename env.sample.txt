# AI Gateway key. Free tier: use a cheap model below (openai/gpt-4o-mini).
AI_GATEWAY_API_KEY=

# AI Gateway model string used by the DurableAgent. Defaults to "openai/gpt-4o-mini" if unset.
AGENT_MODEL=openai/gpt-4o-mini

# --- Slack: interactive approvals (preferred) ---
# Bot token (xoxb-...) with chat:write, plus the channel to post approvals to.
# With these set, reviewers approve with buttons inside Slack and the approval
# token never travels through a browser URL.
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=
# Signing secret from Basic Information; required to verify Slack's requests.
SLACK_SIGNING_SECRET=

# Signs the approval receipts that let issueRefund prove a human said yes.
# Falls back to SLACK_SIGNING_SECRET; one of the two is required in production.
APPROVAL_SIGNING_SECRET=

# --- Slack: incoming webhook (fallback) ---
# Used only when SLACK_BOT_TOKEN/SLACK_CHANNEL_ID are absent. Posts a link to
# the web approval page instead of buttons.
SLACK_WEBHOOK_URL=

# Public base URL used to build the fallback approval link.
APP_URL=http://localhost:3000

# Policy thresholds (all optional, defaults shown)
REFUND_AUTO_APPROVE_MAX_USD=100
REFUND_MAX_MONTHLY_COUNT=2
HIGH_VALUE_THRESHOLD_USD=1000
